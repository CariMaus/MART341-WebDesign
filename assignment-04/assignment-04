Browsers are used as search engines. I use Safari and Google Chrome.

A markup language is how the browser reads and presents information on a site. HTLM or Markdown are two common languages used in development. 
